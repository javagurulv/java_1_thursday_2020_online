Этот пустой конструктор без параметров который может явно не указываться. При создании класса
происходит только вызов конструктора суперкласса.

Если есть класс:
class Example {

}

Это эквивалентно:
class Example
{
     Example()
     {
          super;
     }
}
Здесь класс предка явно не указан, а по умолчанию все классы Java наследуют класс Object
поэтому вызывается конструктор класса Object.