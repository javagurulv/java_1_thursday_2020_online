Найти в интеренете ответ на вопрос:
Как откомпилировать код с помощью JDK компилятора javac,
без помощи среды разработки (из консоли)?

Answer:
At the command line, specify the path of the folder with the compilation file ->
cd C: (path to the folder with the file) , press enter.
After which we enter the command on the cmd -> javac (file name.java).
The file is compiled